@model ContactInfo
@{
    ViewData["Title"] = "İletişim Bilgileri";
    Layout = "~/Views/AdminLayout/Layout.cshtml";
}

<div class="container mt-4">
    <hr />

    <div class="row g-4">
        <!-- Sol: Bilgiler -->
        <div class="col-12 col-lg-5">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <h5 class="card-title mb-4">İletişim Bilgileri</h5>

                    <div class="d-flex align-items-start mb-3">
                        <i class="bi bi-geo-alt fs-4 me-3"></i>
                        <div>
                            <div class="fw-semibold">Adres</div>
                            <div class="text-muted">@Model.Address</div>
                        </div>
                    </div>

                    <div class="d-flex align-items-start mb-3">
                        <i class="bi bi-telephone fs-4 me-3"></i>
                        <div>
                            <div class="fw-semibold">Telefon</div>
                            <div>
                                <a href="tel:@Model.PhoneNumber">@Model.PhoneNumber</a>
                            </div>
                        </div>
                    </div>

                    <div class="d-flex align-items-start mb-3">
                        <i class="bi bi-envelope fs-4 me-3"></i>
                        <div>
                            <div class="fw-semibold">E-posta</div>
                            <div>
                                <a href="mailto:@Model.Email">@Model.Email</a>
                            </div>
                        </div>
                    </div>

                    <div class="d-flex align-items-start">
                        <i class="bi bi-map fs-4 me-3"></i>
                        <div class="w-100">
                            <div class="fw-semibold">Harita Linki</div>
                            <div class="text-truncate">
                                <a href="@Model.MapUrl" target="_blank" rel="noopener">@Model.MapUrl</a>
                            </div>
                        </div>
                    </div>

                </div>
            </div>
        </div>

    
        <div class="col-12 col-lg-7">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <h5 class="card-title mb-3">Konum</h5>
                  
                    <div class="ratio ratio-4x3">
                        <iframe src="@Model.MapUrl"
                                style="border:0;"
                                allowfullscreen=""
                                loading="lazy"
                                referrerpolicy="no-referrer-when-downgrade">
                        </iframe>
                    </div>

                    <small class="text-muted d-block mt-2">
                        Harita görünmüyorsa, lütfen <strong>MapUrl</strong> alanına <em>Embed</em> (yerleştir) linki gir.
                        Google Maps &rarr; Paylaş &rarr; Haritayı yerleştir &rarr; <em>src</em> içindeki link.
                    </small>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="col-sm-12 mt-3 text-end">

    <a href="/Contact/EditContact/@Model.ContactInfoId" class="btn btn-success d-inline-flex"><i class="ti ti-circle-check me-1"></i>Düzenle</a>
</div>

